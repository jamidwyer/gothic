<div id="page" class="{{ attributes.class }}"{{ attributes }}>

  <!-- ______________________ HEADER _______________________ -->


  {% if main_menu or secondary_menu %}
    <nav id="navigation" class="menu{% if main_menu %} with-primary{% endif %}{% if secondary_menu %} with-secondary{% endif %}">
      {{ main_menu }}
      {{ secondary_menu }}
    </nav> <!-- /navigation -->
  {% endif %}

  <!-- ______________________ MAIN _______________________ -->


  <div id="main" class="clearfix">
     <div class="container">
      {% if site_name or site_slogan %}
        {% if site_name %}
          {% if title %}
            <div id="site-name">
               <a href="http://www.amazon.com/Priests-Lagash-David-Jordan-ebook/dp/B004G08YIU/">{{  site_name }}</a>
            </div>
          {% else %}{# Use h1 when the content title is empty #}
            <h1 id="site-name">
              <a href="{{ front_page }}" title="{{ 'Home'|t }}" rel="home">{{  site_name }}</a>
            </h1>
          {% endif %}
        {% endif %}


      {% endif %}

      {% if page.header %}
        <div id="header-region">
          {{ page.header }}
        </div>
      {% endif %}
      <section id="content">

        {% if breadcrumb or title or messages or tabs or action_links %}
          <div id="content-header">

            {{ breadcrumb }}

            {% if page.highlighted %}
              <div id="highlighted">{{ page.highlighted }}</div>
            {% endif %}

            {% if action_links %}
              <ul class="action-links">{{ action_links }}</ul>
            {% endif %}

          </div> <!-- /#content-header -->
        {% endif %}

          {{ page.content }}

        {{ feed_icons }}

      </section> <!-- /content-inner /content -->

      {% if page.sidebar_first %}
        <aside id="sidebar-first" class="column sidebar first">
          {{ page.sidebar_first }}
        </aside>
      {% endif %} <!-- /sidebar-first -->

      {% if page.sidebar_second %}
        <aside id="sidebar-second" class="column sidebar second">
          {{ page.sidebar_second }}
        </aside>
      {% endif %} <!-- /sidebar-second -->
    </div> <!-- /container -->
  </div> <!-- /main -->

  <!-- ______________________ FOOTER _______________________ -->

  {% if page.footer %}
    <footer id="footer">
      <div class="container">
        {{ page.footer }}
      </div>
    </footer> <!-- /footer -->
  {% endif %}

</div> <!-- /page --></a>